#include <iostream>
#include <cmath> // #include<math.h>
using namespace std;

// using the math library
int main()
{
    // powers and roots
    // a new library <cmath>
    // pow (base, exponent) -> base power exponent 3^2; 6 ^ 10
    int a = 3;
    int b = 2;

    int d = pow(6,10); // integer
    cout << d << endl;

    cout << sqrt(7) << endl;
    
    return 0;
}